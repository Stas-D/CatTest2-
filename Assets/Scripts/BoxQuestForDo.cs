using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*   
public class BoxQuestForDo : MonoBehaviour
{
    public Animator anim; 
    public AnimationClip a;
    public bool used;
    public GameObject box;
    
  
void Start () 
{
     
         anim = GetComponent<Animator>();

}

     void Update ()
     {
         anim.SetBool("used", used);
 
         if (Input.GetKey(KeyCode.But))
         {
             used = true;
             Debug.Log("door is open");
         }
        
     }
 }





    void On_ButtonUp (string ButtonDo) {

        if(ButtonDo == "ButtonDo")
        {
          anim = gameObject.GetComponent<Animator>();
          used = true;
          anim.Play(a.name);
        }
    }
}

   void On_ButtonUp (string ButtonDo, GameObject box)
 {
     // thats the button and string
     if (ButtonDo == "ButtonDo")
     {
          
         anim.("true");

         GameObject.Find("Cube").SetActive (true);
     }
 }
 }*/




